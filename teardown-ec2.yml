---
 - hosts: localhost
   gather_facts: false

   tasks:
   - name: Terminate instance
     ec2_instance:
       state: stopped
       region: "{{ ec2_region }}"
       filters:
         instance-state-name: running
         key-name: dockeransible
         tag-name: dockeransible